昆布在庫管理 v165.9.9 取消解除安定化

上書き/追加するファイル:
1. index.html              上書き
2. sw-v160.js              上書き
3. kombu-cancel-restore-v1.js  新規追加

修正内容:
- 出荷依頼履歴の「取消解除」で、取消済グループへ再度戻る問題を修正。
- FAXBOX側の canceled ジョブを安全な held 状態へ移し、取消同期による再取消を防止。
- 取消解除だけでFAXを再送しない。
- 出荷依頼本体 / 履歴 / snapshot の取消フラグを同時に解除。
- 過去テスト等で出荷IDが再利用された場合、別の古いFAXBOXジョブの取消を無視。
